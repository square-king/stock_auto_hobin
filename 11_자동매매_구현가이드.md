# 자동매매 전략 통합 가이드

> Hobeen Macro Lab 블로그 내용 기반 자동매매 프로그램 구현 가이드

## 구현 대상 전략 (우선순위 순)

### 1순위: 20일선 눌림목 전략 ⭐
**1달 수익률: +71.48%** (가장 높은 성과)

```python
# 핵심 로직
매수조건:
- 최근 1개월 +30% 이상 급등 이력
- Volume Dry-up (거래량 감소)
- 20일선 근접 + 지지 신호 (아래꼬리/양봉전환)

매도조건:
- 전고점 50% 분할익절
- 전일저가 이탈 or 10일선 이탈 시 전량청산

손절: 20일선 종가 이탈 or 진입가 -5%
```

### 2순위: 엔벨로프 20-20 전략
**1달 수익률: +9.80%**

```python
# 핵심 로직
매수조건:
- 가격 < MA20 × 0.80 (하한선 터치)

매도조건:
- 가격 >= MA20 (중심선 터치)

손절: 진입가 -5%
```

### 3순위: 스토캐스틱 눌림목 전략
**1달 수익률: +7.46%**

```python
# 핵심 로직
매수조건:
- 추세: 가격 > SMA20 and SMA20 > SMA60
- 과매도: Stoch_K(12,5,5) < 20
- 트리거: %K > %D (골든크로스)

매도조건:
- Stoch_K > 80이면 50% 익절
- %K < %D (데드크로스)면 전량청산
```

### 4순위: 수급 전략
**1달 수익률: +7.96%**

```python
# 핵심 로직
매수조건:
- 외국인 + 기관 동시 순매수
- 순매수 점유율 > 10%
- 양봉 마감

매도조건:
- 전일저가 이탈
- 3일 연속 순매도 전환
```

---

## 공통 리스크 관리

```python
# 자금관리
max_positions = 2  # 최대 동시 보유 종목
risk_per_trade = 0.01  # 1회 손실 한도 = 자본금의 1%
position_size = risk_per_trade * capital / (entry_price - stop_price)

# 공통 규칙
no_averaging_down = True  # 물타기 금지
mechanical_stop_loss = True  # 기계적 손절
```

---

## 지표 구현 요약

### MA (이동평균)
```python
SMA20 = price.rolling(20).mean()
SMA50 = price.rolling(50).mean()
SMA60 = price.rolling(60).mean()
```

### 엔벨로프
```python
envelope_upper = SMA20 * 1.20
envelope_lower = SMA20 * 0.80
```

### 스토캐스틱 Slow (12,5,5)
```python
low_min = low.rolling(12).min()
high_max = high.rolling(12).max()
raw_k = (close - low_min) / (high_max - low_min) * 100
stoch_k = raw_k.rolling(5).mean()  # %K
stoch_d = stoch_k.rolling(5).mean()  # %D
```

### 볼린저밴드
```python
bb_mid = SMA20
bb_std = price.rolling(20).std()
bb_upper = bb_mid + (2 * bb_std)
bb_lower = bb_mid - (2 * bb_std)
bb_width = (bb_upper - bb_lower) / bb_mid * 100
```

---

## 스크리너 구현 (트레이딩뷰 대체)

### 눌림목 후보 스크리너
```python
def screen_pullback_candidates(stocks):
    candidates = []
    for stock in stocks:
        # 1. 최근 급등 체크
        monthly_return = (price[-1] - price[-20]) / price[-20]
        if monthly_return < 0.30:
            continue
        
        # 2. 52주 고가 근처
        if price[-1] < high_52w * 0.80:
            continue
        
        # 3. 거래대금 필터
        if avg_volume_20d < 5e9:  # 50억
            continue
        
        # 4. 추세 유지
        if price[-1] < SMA20[-1]:
            continue
        
        # 5. Volume Dry-up
        if volume[-5:].mean() > volume[-20:].mean():
            continue
        
        candidates.append(stock)
    
    return candidates
```

---

## 데이터 소스 권장

| 데이터           | 소스                         |
| ---------------- | ---------------------------- |
| 한국 주식        | KIS API, 키움 API            |
| 미국 주식        | Yahoo Finance, Alpha Vantage |
| 외국인/기관 수급 | KIS API, 증권사 MTS          |
| 실시간 시세      | 증권사 API                   |

---

## 구현 순서 권장

1. **데이터 수집 모듈** - 가격, 거래량, 수급 데이터
2. **지표 계산 모듈** - MA, 스토캐스틱, 엔벨로프, 볼린저
3. **스크리너 모듈** - 전략별 후보 필터링
4. **시그널 생성 모듈** - 매수/매도 신호
5. **주문 실행 모듈** - 증권사 API 연동
6. **리스크 관리 모듈** - 포지션 사이징, 손절
7. **로깅/모니터링** - 매매 기록, 알림

---

## 백테스트 권장 기간

- 최소: 1년
- 권장: 3년 (상승장/하락장/횡보장 포함)
- 주의: 과최적화 방지

---

## 다음 단계

1. 눌림목 전략 파이썬 구현
2. 백테스트 프레임워크 설정
3. 페이퍼 트레이딩 (모의투자)
4. 실전 적용

---

> **"감정 개입 없는 기계적 매매, 규칙 기반 리스크 관리, 전략을 숫자로 검증"**
